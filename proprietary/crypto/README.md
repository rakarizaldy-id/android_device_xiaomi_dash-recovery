# Stock crypto payloads

Place locally extracted DASH stock crypto payloads under this directory before building.

The expected file set and destination layout are defined by `crypto_payload.mk`.

Use `stock_crypto_sha256sums.txt` to verify the extracted files against the validated stock baseline.

The payload binaries are intentionally excluded from Git and must come from matching DASH stock firmware.
