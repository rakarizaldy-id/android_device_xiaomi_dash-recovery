# Stock platform files

Place locally extracted DASH stock platform files in this directory before building.

Expected local files:

```text
vendor_boot.img
dtb/dash-id303.dtb
```

These files are intentionally excluded from Git. See the repository-level `STOCK_PAYLOADS.md` for the validated baseline and hashes.
